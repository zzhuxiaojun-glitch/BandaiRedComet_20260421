# 17. 接下来推进计划（v0.2.0 发布前）

**最后更新**：2026-04-28（commit `3b3a79a` 之后）
**目标**：用 2-4 小时把 GUI 推进到能打包成 exe 给朋友直接用的状态。

---

## 17.1 当前状态速读

### ✅ 已完成（不再需要做的）

| 模块 | 状态 |
|---|---|
| 核心抢购（HAR 验证 + 真订单 1416249） | ✅ |
| GUI 4 视图（表单 / 倒计时 / 抢中 / 失败诊断） | ✅ |
| URL 自动识别 SPU | ✅ |
| 错误卡片化（14 类） | ✅ |
| 从 HAR 选商品 modal | ✅ |
| **关键词搜索商品**（实时调 spu/query） | ✅ |
| **CK 自动验证 + 状态徽章** | ✅ |
| **从 HAR 一键导入 CK** | ✅ |
| **SQLite 历史 · 我的订单 + 搜索历史** | ✅ |
| 50/50 单元测试 | ✅ |

### ⏳ 还要做的

1. **你**：自己跑一遍 GUI 验证（10 min）
2. **你**：真抢购演练（30-60 min）
3. **我**：Phase 3 打 exe（2-3h，你不参与）
4. **你**：在 Windows 测一下 exe 双击能跑（5 min）

### 🚫 暂缓的事（v0.3.0+ 再说）

- ❌ 内置 mitmproxy 后台抓包（4-5h，复杂；先看 HAR 导入用着够不够）
- ❌ Phase 2.4 多商品队列（万代实战不刚需）
- ❌ 抢中后调 getOrderDetail 拿 BD26 单号（订单 ID 已经够用）

---

## 17.2 现在就做的事（你 → 我 → 你 → 我）

### 步骤 1：你测试新 GUI（10 min）

打开 GUI：

```bash
bdgui
```

**应该看到的新东西**（截图给我对照也行）：

| 区域 | 新元素 |
|---|---|
| 表单顶部 | 工具栏：`📦 我的订单`、`🔍 搜索历史` 两个链接按钮 |
| ① 账号 卡片 | CK 输入框下出现状态徽章（黄色 ✅ 有效 / 粉色 🔑 失效 / 蓝色 🔄 验证中） |
| ① 账号 hint | 多了 `📁 从 HAR 导入 CK` 链接 |
| ② 商品 卡片 | 顶部多了关键词搜索行 |

**几个交互测试**：

```
□ 启动后 CK 状态徽章自动出现（如果 .env 或上次有保存 CK）
□ 在搜索框打 "里歇尔" 回车 → 弹 modal 显示 1 个商品
□ 在搜索框打 "高达" 回车 → 弹 modal 显示几百个商品列表
□ 点 modal 里任一商品 → SPU 自动填进 ② 卡片 + 闪绿 ✓
□ 点 "🔍 搜索历史" → 看到刚搜的"里歇尔"和"高达"
□ 点搜索历史里的关键词 → 自动重新搜
□ 点 "📦 我的订单" → 应该显示空（还没真抢过；之后真抢了会出现）
□ 故意把 CK 改坏一个字符 → 失焦 600ms 后徽章变粉色 🔑
□ 点 "📁 从 HAR 导入 CK" → 选你的 HAR 文件 → CK 自动填回
```

测完告诉我哪些 ✓ 哪些 ✗。如果都过就直接进步骤 2。

---

### 步骤 2：真抢购演练（30-60 min）

按 [`16_真抢购演练SOP.md`](./16_真抢购演练SOP.md) 走，但**有两点更新**（用上面新功能更轻松）：

#### 更新点 A：步骤 2 改用搜索框（不必再 HAR picker）

原 SOP §16.3 步骤 2 让你用 "📁 从 HAR 选商品"，**现在可以直接搜**：

```
② 商品 卡片 顶部 → 搜索框输入 "电王腰带"
→ 回车
→ modal 弹出，找到 SPU 1268
→ 点击它自动填
```

更省事。

#### 更新点 B：步骤 1 CK 不用手动粘

启动 GUI 后看 CK 状态徽章：
- ✅ 黄色 = 不用动
- 🔑 粉色 = 点 "📁 从 HAR 导入 CK" → 选最新 HAR

#### 演练完反馈表（贴这一段给我）

```
启动新 GUI ✓ / ✗ ____
搜索"电王腰带" ✓ / ✗ ____
点搜索结果填 SPU 1268 ✓ / ✗ ____
预检 ✓ / ✗ ____
设 snipe_time +1 分钟 ✓ / ✗ ____
点开始抢购 ✓ / ✗ ____
倒计时 / 进度条 ✓ / ✗ ____
抢中 → 拿到 prepay_id ✓ / ✗ ____
   订单 ID = ________
   失败的话错误图标 + 标题 = ________
回到表单 → 点 "📦 我的订单" 看到刚才那笔 ✓ / ✗ ____
微信小程序"我的订单"看到那笔 ✓ / ✗ ____
通知通道（如果配了）收到推送 ✓ / ✗ ____
```

**演练完不要付款**（订单 15 min 自动取消）。

---

### 步骤 3：我打 Phase 3 exe（你等通知）

收到你的"演练全过"反馈后，我会做：

1. 写 `万代抢购器.spec` PyInstaller 配置
2. `scripts/build_exe.ps1` Windows 打包脚本
3. 添加图标（夏亚扎古头像，等你给 GEMINI_API_KEY 我用 nano-banana-pro 生成；或你自己 PS 给我一个 .ico）
4. 在 WSL 跑一次 `pyinstaller --onefile` 看会不会过（WSL 跑 Linux exe 验证打包链路）
5. 输出 `dist/万代抢购器.exe`，让你在 Windows 双击测

预计 2-3h。**这步你完全不参与**，我做完会有一个新 commit + GitHub Release v0.2.0 附 exe。

---

### 步骤 4：你测 Windows 双击 exe（5 min）

我打包后给你：

```
□ 把 万代抢购器.exe 复制到 Windows 任意目录
□ 双击启动
□ 出现窗口（可能首次启动 1-3 秒，PyInstaller 解压临时文件）
□ 走一遍登录 → 搜商品 → 预检 → 验证流程是否一致（不用真抢）
□ 关掉重开，看 CK 还在
```

如果 OK → 发 v0.2.0 release 给朋友 → **项目落地完成**。

如果出 bug → 反馈我修 → 重打。

---

## 17.3 给朋友的交付物（v0.2.0）

- `万代抢购器.exe`（约 50 MB 单文件，含 Python + 所有依赖）
- 一份简版 `朋友使用指南.md`（重写 [10 号](./10_使用指南.md) GUI 部分给小白看）
- GitHub Release page：[https://github.com/zzhuxiaojun-glitch/BandaiRedComet_20260421/releases](https://github.com/zzhuxiaojun-glitch/BandaiRedComet_20260421/releases)

朋友拿到 exe 后只需：
1. 双击打开
2. 抓一次 HAR（或用朋友给的"上号软件"）→ 点 GUI 里"📁 从 HAR 导入 CK"
3. 搜要抢的商品名
4. 选 SKU + 地址 + 设抢购时间
5. 等开抢，点击"开始抢购"

完全不用 Python / 命令行 / yaml。

---

## 17.4 v0.3.0+ 计划（远期）

只在朋友真正用上 v0.2.0 反馈痛点后再决定要不要做：

- **内置 mitmproxy**（如果朋友抓 HAR 太烦）
- **多商品候补队列**（如果朋友抢一个失败想自动抢备选）
- **支付完成检测 / 订单状态自动刷新**（getOrderDetail 后台轮询）
- **数据导出**（订单和搜索历史导出 CSV）
- **浅色 / 深色主题切换**

---

## 17.5 你现在要做的第一件事

**就是步骤 1**：

```bash
bdgui
```

跑起来看一眼。10 分钟之内告诉我结果，我们继续步骤 2 演练。
